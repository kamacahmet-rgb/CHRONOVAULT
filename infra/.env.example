# infra/.env — kopyala: cp .env.example .env  (değerleri üretimde güçlendirin)
# Docker Compose bu dosyayı okur.

POSTGRES_USER=turkdamga
POSTGRES_PASSWORD=degistir_guclu_parola_en_az_24_karakter
POSTGRES_DB=turkdamga

REDIS_PASSWORD=degistir_baska_guclu_parola

# Uygulama (ileride backend bu compose ile aynı ağda çalıştığında)
# DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
# REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
